# Python dependencies for the VittVanni backend (Python 3.10+).
# Installed by Vercel for the serverless function in api/index.py.
# The frontend needs no packages - pure static HTML/CSS/JS.

# --- Core web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- ORM + database ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# --- Validation + configuration ---
pydantic>=2.10,<3.0
email-validator>=2.2,<3.0
pydantic-settings>=2.6,<3.0

# --- Authentication (JWT + password hashing) ---
python-jose[cryptography]>=3.3,<4.0
bcrypt>=4.1,<6.0

# --- HTTP forms + environment ---
python-multipart>=0.0.9,<1.0
python-dotenv>=1.0,<2.0

# --- AI service hooks (reserved; Gemini REST + builtin fallback used) ---
anthropic>=0.40,<1.0
websockets>=12,<18

# --- Testing ---
pytest>=8.0,<9.0
httpx>=0.27,<1.0